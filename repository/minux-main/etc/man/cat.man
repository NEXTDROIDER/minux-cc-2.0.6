cat, displays a file
executable location: 
'/bin/cat.sh'  
alias: 'cat'
cat is part of base

cat is used to display 
the contents of a file
without having to use 
the 'edit' command,it 
simply reads a file 
and displays it

syntax : 
'cat filename option'

option parameters:
p : providing 'p' after
the filename will cause
it to pause every so 
often to allow the user
to read the file.
		 
options are optional.